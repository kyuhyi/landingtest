# 🧪 회원가입 테스트 가이드

## ✅ 준비 완료 사항

1. **디버깅 로그 추가 완료**
   - 회원가입 전 과정에 상세 로그 추가
   - Firebase Auth 계정 생성 로그
   - Firestore 저장 로그
   - 에러 추적 로그

2. **서버 실행 완료**
   - 개발 서버: http://localhost:3000
   - 로그 출력 활성화

---

## 🔬 테스트 절차

### 1. 브라우저 콘솔 열기
- `F12` 키 또는 `Ctrl+Shift+I`
- **Console** 탭 선택
- 로그가 보이도록 설정

### 2. 회원가입 페이지 접속
```
http://localhost:3000/register
```

### 3. 테스트 정보 입력
```
이름: 테스트유저
이메일: test@example.com
비밀번호: test1234
비밀번호 확인: test1234
```

### 4. 회원가입 버튼 클릭 후 콘솔 확인

**정상 동작 시 콘솔 로그:**
```
🔥 회원가입 시작: {email: "test@example.com", name: "테스트유저"}
📝 Firebase Auth 계정 생성 중...
✅ Firebase Auth 계정 성공: [UID]
💾 Firestore에 사용자 프로필 저장 중...
📤 저장할 데이터: {uid: "[UID]", email: "test@example.com", name: "테스트유저", role: "user"}
🔵 createUser 호출됨: {uid: "[UID]", email: "test@example.com", ...}
🔵 Firestore에 저장할 문서: {collection: "users", docId: "[UID]", data: {...}}
✅ createUser 성공
✅ Firestore 사용자 프로필 저장 성공
📥 저장된 프로필 조회: {uid: "[UID]", email: "test@example.com", ...}
```

---

## 🔍 에러별 대응

### Case 1: "permission-denied" 에러
**증상:**
```
❌ createUser 실패: FirebaseError: Missing or insufficient permissions
```

**원인:** Firestore 보안 규칙이 아직 적용되지 않음

**해결:**
1. Firebase Console → Firestore Database → 규칙
2. 다음 코드로 교체:
```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
```
3. **게시** 클릭
4. 1-2분 대기 후 다시 시도

### Case 2: "auth/email-already-in-use"
**증상:**
```
❌ 회원가입 오류: FirebaseError: The email address is already in use
```

**해결:** 다른 이메일로 시도
```
test2@example.com
test3@example.com
```

### Case 3: Firebase Auth는 성공했으나 Firestore 저장 실패
**증상:**
```
✅ Firebase Auth 계정 성공: [UID]
❌ createUser 실패: [에러]
```

**확인 사항:**
1. Firebase Console → Firestore Database가 생성되어 있는지 확인
2. 보안 규칙이 제대로 설정되어 있는지 확인

---

## ✅ 성공 확인

### 1. Firebase Authentication 확인
1. Firebase Console 열기
2. **Authentication** 클릭
3. **Users** 탭에서 `test@example.com` 확인

### 2. Firestore Database 확인
1. Firebase Console → **Firestore Database**
2. **users** 컬렉션 클릭
3. UID 문서 확인
4. 필드 확인:
   - ✅ uid
   - ✅ email: test@example.com
   - ✅ name: 테스트유저
   - ✅ role: user
   - ✅ createdAt
   - ✅ updatedAt

### 3. 로그인 테스트
1. `http://localhost:3000/login` 접속
2. 방금 가입한 계정으로 로그인
3. 홈페이지로 리다이렉트 확인
4. 우측 상단에 "테스트유저" 이름 표시 확인

---

## 📊 디버깅 체크리스트

모든 로그를 확인하며 어느 단계에서 실패하는지 파악:

- [ ] 🔥 회원가입 시작 로그 출력
- [ ] 📝 Firebase Auth 계정 생성 중 로그 출력
- [ ] ✅ Firebase Auth 계정 성공 로그 출력 (UID 포함)
- [ ] 💾 Firestore 저장 중 로그 출력
- [ ] 📤 저장할 데이터 로그 출력
- [ ] 🔵 createUser 호출됨 로그 출력
- [ ] 🔵 Firestore 저장할 문서 로그 출력
- [ ] ✅ createUser 성공 로그 출력
- [ ] ✅ Firestore 프로필 저장 성공 로그 출력
- [ ] 📥 저장된 프로필 조회 로그 출력

**모든 체크리스트 완료 시 → 회원가입 성공! 🎉**

---

## 🚨 여전히 실패하면

콘솔의 **전체 에러 로그**를 복사해서 알려주세요.
특히 다음 정보가 중요합니다:

1. 에러 발생 시점 (어떤 로그 다음에 에러가 발생했는지)
2. 정확한 에러 메시지
3. Firebase Console의 Firestore 규칙 현재 상태
4. Firebase Console의 Authentication 활성화 상태

---

## 📝 테스트 결과 기록

회원가입 시도 후 다음을 확인:

```
[ ] 콘솔에 모든 로그가 순서대로 출력됨
[ ] Firebase Authentication에 계정 생성됨
[ ] Firestore users 컬렉션에 문서 생성됨
[ ] 로그인 성공
[ ] 사용자 이름이 화면에 표시됨
```

**모두 체크되면 Firebase 연동 완전히 성공! ✅**
